import 'dart:async';
import 'package:flutter/material.dart';

class paginaRestaurantes extends StatelessWidget {
  const paginaRestaurantes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("PAGINA RESTAURANTES"),
    );
  }
}