import 'dart:async';
import 'package:flutter/material.dart';

class widget_perfil extends StatelessWidget {
  const widget_perfil({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("PAGINA PERFIL"),
    );
  }
}